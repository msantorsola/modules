process {

    withName: GAMETES_GENERATEMODELS {
        ext.args = [
            "-p ${params.pevalence}",
            "-a ${params.attributeAlleleFrequency}",
            "-d ${params.useOddsRatio}",
            "-f ${params.proportion}"
        ].join(' ').trim()

        ]
    }
}

